import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'logic.dart';

class Complete_userinfoPage extends StatelessWidget {
  final logic = Get.put(Complete_userinfoLogic());
  final state = Get.find<Complete_userinfoLogic>().state;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
